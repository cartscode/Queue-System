testing apache if running!
<?php
phpinfo();
?>
____________________________________________________________________________
link for opening try
    http://localhost/Project%20in%20CC105/informationmanagement/informationmanagement.php

        http://localhost/Project%20in%20CC105/logres/login.php

    http://localhost/Project%20in%20CC105/home/

__________________________________________________________________________

Console sa Xampp(let's go)

CREATE database hospital_queuedb;	

USE hospital_queuedb;

CREATE TABLE IF NOT EXISTS patients (
    id INT(11) AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    birthdate DATE NOT NULL,
    condition_text TEXT NOT NULL,
    type ENUM('regular', 'priority') NOT NULL DEFAULT 'regular',
    doctor_type ENUM('general_practitioner', 'pediatrician', 'dermatologist', 'cardiologist', 'orthopedic', 'gynecologist') NOT NULL,
    room VARCHAR(10) NOT NULL,
    registered_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    status ENUM('Waiting', 'Being Treated', 'Completed') NOT NULL DEFAULT 'Waiting'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE users (
    id INT(11) NOT NULL AUTO_INCREMENT PRIMARY KEY,
    email VARCHAR(150) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL,
    recovery_question VARCHAR(255) NOT NULL,
    recovery_answer VARCHAR(255) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);


_________________________________________________________________________________
things I need to fix(Langaw sa source code) -carter


1.Delete btn in table functional connected to database ✔️
2.average time waiting clock fix It. ✔️
3.remove refresh every 5scs ✔️
4.Fixed FIFO logic in the table✔️
5.Vertical & HOrizontal scrollbar (UI) 
6.Action function bug(it should change the attribute in database when it clicks)(priority)✔️
7.ContactForm bugs in UI responsive Hamburger ✔️
8.Bugs changing the link of btn register✔️
9. We need to create another copy file for home,aboutus,contactus,services pages with changes of btn log out instead of register when it's already log in.